#include "main.h" // Include the header file "main.h"
#include <stdio.h> // Include the standard input/output library

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */
int main(void)
{
	printf("%d\n", _strlen("My Dyn Lib")); // Print the length of the string "My Dyn Lib"
	return (EXIT_SUCCESS); // Return EXIT_SUCCESS to indicate successful execution
}
