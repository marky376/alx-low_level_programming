#include<stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	// Loop through numbers from 0 to 9
	for (i = 0; i < 10; i++)
		printf("%d", i);

	printf("\n");

	return (0);
}
