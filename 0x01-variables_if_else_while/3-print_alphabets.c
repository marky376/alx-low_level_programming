#include <stdio.h>
/**
 * main - main function
 *
 * Return:always 0
 */

int main(void);
{
	char = i;
	for (i = 'b'; i <= 'z'; i++)
		putchar(i);
	for (i = 'B'; i <= 'Z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
