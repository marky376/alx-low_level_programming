/**
 * delete_nodeint_at_index - function that deletes a node at index of a list
 * @head: the head pointer of the given list
 * @index: the index of the node that should be deleted
 *
 * This function deletes a node at the specified index in a singly linked list.
 * It takes the head pointer of the list and the index as parameters.
 * If the index is valid, the function removes the node at that index and frees the memory.
 * If the index is out of range or the list is empty, the function returns -1.
 *
 * Return: 1 if the node was successfully deleted, -1 otherwise
 */
