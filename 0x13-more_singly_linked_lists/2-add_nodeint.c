/**
 * add_nodeint - function that adds a node at the beginning of a list
 * @head: the pointer to the head of a list
 * @n: the value int to be put in the node
 *
 * This function creates a new node with the given value 'n' and adds it at the
 * beginning of the list pointed to by 'head'. If 'head' is NULL, the function
 * returns NULL. If memory allocation fails, the function also returns NULL.
 *
 * Return: The newly created node.
 */
