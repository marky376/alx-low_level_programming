/**
 * infinite_add - Adds two numbers requested as strings
 * @n1: The first number as a string
 * @n2: The second number as a string
 * @r: The buffer to store the result
 * @size_r: The size of the buffer
 *
 * This function takes two numbers represented as strings, adds them together,
 * and stores the result in a buffer. The size of the buffer is specified by
 * the parameter size_r. The function returns a pointer to the result.
 *
 * Return: A pointer to the result
 */
