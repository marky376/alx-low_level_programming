/**
 * _strcmp - Compares two strings
 * @s1: The first string to compare
 * @s2: The second string to compare
 *
 * This function compares the characters of two strings, s1 and s2, until a
 * difference is found or the end of either string is reached. It returns an
 * integer value indicating the difference between the first differing
 * characters in the two strings.
 *
 * Return: 0 if the strings are equal, a negative value if s1 is less than s2,
 *         or a positive value if s1 is greater than s2.
 */
