/**
 * set_string - sets the value of a pointer to a char
 * @s: pointer to a pointer to a char (double pointer)
 * @to: pointer to the string to set the pointer to
 *
 * Description:
 *   This function takes a double pointer to a char and a pointer to a string.
 *   It sets the value of the double pointer to the address of the string.
 *   This effectively changes the value of the original pointer to point to a new string.
 */
