#include <stdio.h>

/**
 * main - main function
 *
 * This is the main function of the program.
 * It prints the message "with proper grammar, but the outcome is a piece of art,"
 * and returns 0.
 *
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
